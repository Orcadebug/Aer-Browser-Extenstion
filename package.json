{
  "name": "aer-chrome-extension",
  "version": "1.0.0",
  "description": "Chrome extension for Aer context capture",
  "scripts": {
    "build": "webpack --mode production --entry ./background.js --output-path . --output-filename background.bundle.js && webpack --mode production --entry ./prompt_inject.js --output-path . --output-filename prompt_inject.bundle.js"
  },
  "dependencies": {
    "convex": "^1.27.0",
    "tweetnacl": "^1.0.3",
    "tweetnacl-util": "^0.15.1"
  },
  "devDependencies": {
    "webpack": "^5.89.0",
    "webpack-cli": "^5.1.4"
  }
}
